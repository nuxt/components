<%= JSON.stringify(options.getComponents().map(component => {
  const c = { ...component }
  delete c.import
  c.filePath = c.filePath.replace(nuxtOptions.srcDir, '').replace(/^[\/\\]/, '')
  return c
}), null, 2) %>
