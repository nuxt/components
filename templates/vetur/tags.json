<%
const tags = {}
for (const c of options.getComponents()) {
  tags[c.pascalName] = {
    description: `Auto imported from ${c.filePath.replace(nuxtOptions.rootDir, '').replace(/^[\/\\]/, '')}`
  }
}
%><%= JSON.stringify(tags, null, 2) %>
