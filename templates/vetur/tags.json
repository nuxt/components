<%
const tags = {}
for (const c of options.getComponents()) {
  tags[c.pascalName] = {
    description: `Auto imported from ${c.filePath.replace(nuxtOptions.srcDir, '').replace(/^[\/\\]/, '')}`
  }
}
%><%= JSON.stringify(tags, null, 2) %>
